{
  "name": "batch-relay-dapp",
  "version": "1.0.0",
  "description": "Gas optimized batch transaction relayer",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "compile": "truffle compile",
    "deploy": "truffle migrate --network ganache",
    "deploy:reset": "truffle migrate --network ganache --reset",
    "deploy:local": "truffle migrate --network development",
    "console": "truffle console --network ganache",
    "test": "truffle test --network ganache",
    "test:local": "truffle test --network development"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.4.0",
    "@truffle/contract": "^4.6.31",
    "dotenv": "^16.3.1",
    "ethers": "^6.7.1",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "truffle": "^5.11.5"
  }
}
